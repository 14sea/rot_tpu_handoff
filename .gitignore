# Apply-marker is created in the sibling ROT repo, never here, but
# guard against accidental copies.
.rot_tpu_handoff_applied

# Python bytecode
__pycache__/
*.pyc

# qmegawiz writes these error-log scratch files unconditionally
# (even on `qmegawiz --help`).  Always junk.
.cnxerr
qmegawiz_errors_log.txt
.cnx
.claude/
